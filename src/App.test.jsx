import React from 'react';

export default function App() {
  return (
    <div style={{ padding: 24, fontFamily: 'sans-serif' }}>
      <h1>Minimal App — dev test</h1>
      <p>If this loads, the original `App.jsx` content is likely causing the pre-transform recursion.</p>
    </div>
  );
}
